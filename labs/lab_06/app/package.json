{
  "name": "api",
  "version": "1.0.0",
  "description": "A demo api app that connects to cosmos db",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ray Kao",
  "license": "ISC",
  "dependencies": {
    "restify": "^7.2.1"
  }
}
